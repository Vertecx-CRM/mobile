name: vertecx
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  # Estado / Bloc
  equatable: ^2.0.7
  bloc: ^9.0.0
  flutter_bloc: ^9.1.1
  provider: ^6.1.5+1

  # UI / Widgets
  flutter_svg: ^2.2.1
  awesome_dialog: ^3.3.0
  table_calendar: ^3.2.0
  fl_chart: ^1.1.1

  # Utilidades
  intl: ^0.20.2
  dio: ^5.9.0
  http: ^1.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/icons/
    - assets/image/
    - assets/imgs/
